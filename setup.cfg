[develop]
script_dir=$base/lib/turtle_driver
[install]
install_scripts=$base/lib/turtle_driver

[tool:pytest]
markers =
    node: the L2 tier - one framework node driven through the ROS2 objects it declares
timeout = 60
